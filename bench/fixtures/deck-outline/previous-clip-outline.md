# Outline — Clip 2: "Commit Message Conventions" (for reference)

Used as-is for the previous clip in the series. Template/branding notes in
brackets.

1. **Title Slide — "Clip 2: Commit Message Conventions"**
   [Template: dark-navy title card, series logo bottom-left, clip number
   top-right, same template used for every clip in the series.]

2. **Why commit messages matter**
   Bullets: readability for teammates, `git blame` usefulness, changelog
   generation from commit history.

3. **Our format: `type(scope): summary`**
   Explains the convention and the allowed `type` values (feat, fix,
   chore, docs, refactor).

4. **Good vs. bad example**
   Side-by-side: a vague message ("fixed stuff") next to a well-formed one
   ("fix(checkout): retry payment on timeout").

5. **Common mistakes**
   Four bullets: too vague, wrong type, mixing multiple changes in one
   commit, tense inconsistency.

6. **Summary + up next**
   Recap bullets + "Next up: Clip 3, choosing a branching strategy."

[Footer on every content slide: series name + clip number, bottom-right,
same across all clips.]
