# Task: plan how to store this article in the notes vault

Attached: `new-article.md` — an article we want to add to our personal
plant-care notes vault.

Also attached: a snapshot of the current vault relevant to this topic —
`index.md` (the notes index) and three existing notes it links to
(`water-propagation-basics.md`, `soil-propagation-basics.md`,
`leaf-cutting-propagation.md`).

Write a storage plan for the new article: where it should live, what it
should link to (and be linked from), and what frontmatter/metadata it
should have. You don't need to write the final note text — just the plan
(location, connections, metadata) for review before we file it.

Constraints: don't invent vault content that isn't shown to you here (if
you need to guess whether something else in the vault exists, say so
rather than assume it does or doesn't). Don't access anything outside this
directory.
